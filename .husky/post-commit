#!/usr/bin/env sh

# Rebuild @stratif-io/web lib so seeder-studio dev server can resolve it.
# lint-staged may reformat files in apps/web, invalidating the previous build.
cd apps/web && bun run build:lib --silent 2>/dev/null || true
