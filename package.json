{
  "name": "stratifio-oss",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "bun run --filter @stratifio/web dev",
    "build": "bun run --filter @stratifio/web build",
    "preview": "bun run --filter @stratifio/web preview",
    "lint": "bun run --filter @stratifio/web lint",
    "format": "bun run --filter @stratifio/web format",
    "format:check": "bun run --filter @stratifio/web format:check",
    "test": "bun run --filter @stratifio/web test",
    "test:run": "bun run --filter @stratifio/web test:run",
    "test:coverage": "bun run --filter @stratifio/web test:coverage",
    "test:e2e": "bun run --filter @stratifio/web test:e2e",
    "test:e2e:ui": "bun run --filter @stratifio/web test:e2e:ui",
    "docs:dev": "bun run --filter stratifio-docs dev",
    "docs:build": "bun run --filter stratifio-docs build",
    "docs:preview": "bun run --filter stratifio-docs preview",
    "prepare": "husky"
  },
  "devDependencies": {
    "eslint": "^9.15.0",
    "husky": "^9.1.0",
    "lint-staged": "^15.2.0",
    "prettier": "^3.4.0"
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "eslint --fix --flag unstable_config_lookup_from_file",
      "prettier --write"
    ],
    "*.{css,json,md}": [
      "prettier --write"
    ]
  },
  "prepare": "husky",
  "dependencies": {
    "cmdk": "^1.1.1"
  }
}
